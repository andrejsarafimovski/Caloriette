import { assert } from "chai";

// tslint:disable:no-unused-expression
describe("Unit", () => {

    // starter unit tests
    it("Should pass a unit test", async () => {
        assert.isTrue(true);
    });

});
